
## How to run Segmentation example: 
To run the included segmentation example, write:

    python3 -m segmentation.example

After this, the console will ask the user to write the path where it will save the result folder.


## How to run 
# Execute:
./main 21 x.bundles subject  atlas/bundles atlas/atlas_info.txt results_folder indices_folder


